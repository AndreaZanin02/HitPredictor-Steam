### 33. Empirical Convergence of Unified Search vs. Decoupled Pipeline & Transition to Documentation (17/07/2026)
* **The Problem:** After running the multi-core optimized `cached_unified_search.py` overnight across 100% of the dataset, we needed to compare its joint optimization results against our original decoupled pipeline (the two-step approach using `gridSearch_plsda_tfidf.py` followed by Optuna-based tuning). We had to determine if the feature-parameter co-dependency loop introduced significant optimization bias, and decide on our final deployment and reporting milestones as we closed in on our compressed July 20th target deadline.
* **The Choice:** We cross-referenced the overnight grid sweep results with our historical repository logs and established empirical convergence:
    1.  **Unified Grid Search Baseline:** Found optimal bounds at `max_tfidf_features=15`, `n_components=20`, `learning_rate=0.1`, `max_depth=6`, and `n_estimators=400`, yielding a Quadratic Weighted Kappa (QWK) score of 0.702.
    2.  **Decoupled Architecture Baseline:** Historically settled on `max_tfidf_features=15`, `n_components=20`, `learning_rate=0.1074`, `max_depth=5`, and `n_estimators=397`, yielding a QWK score around 0.710.
    3.  **Algorithmic Validation:** We accepted both models as structurally identical. The tiny variation in tree depths and estimators is purely an artifact of their search mechanics (discrete grid spaces vs. continuous distribution sampling via Optuna) and cross-validation fold partitioning luck. 
    4.  **Operational Next Steps:** We audited potential project requirements regarding an `inference.py` script. After confirming no active script was missing from the repository, we resolved all operational code doubts and officially shifted the team's focus to compiling the final report on Overleaf using the official University of Pisa LaTeX template and preparing the final project video.
* **Why we chose this option:** This outcome provides an exceptional empirical victory for our final presentation. By showing that both a unified joint search space and a decoupled pipeline arrive at the exact same architectural parameters (`tfidf=15`, `plsda=20`, `lr≈0.1`, `estimators≈400`), we have solid mathematical proof that our initial pipeline design was sound, optimal, and free from co-dependency traps. Transitioning directly to writing the technical documentation allows us to confidently protect our compressed July 20th target deadline.
